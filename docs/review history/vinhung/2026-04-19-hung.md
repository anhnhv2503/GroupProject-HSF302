# Review task - Ngày 19/04/2026

## Thành viên thực hiện: vinhung

---

## 🚀 Các tính năng đã hoàn thành hôm nay

### 1. Hệ thống Upload & Quản lý Hình ảnh Sản phẩm Nâng cao
- [x] **Giao diện Upload mới**: Thay thế input truyền thống bằng vùng kéo thả (Drag & Drop) hiện đại, hỗ trợ tải lên tối đa 4 ảnh.
- [x] **Xem trước (Preview Grid)**: Hiển thị lưới hình ảnh ngay khi chọn, cho phép xóa từng tấm trước khi lưu.
- [x] **Chọn Ảnh chính (Primary Image)**: Cho phép người dùng chọn 1 tấm ảnh làm ảnh đại diện thông qua icon Ngôi sao.
- [x] **Gallery Modal**: Tích hợp thư viện Swiper.js để trình chiếu ảnh sản phẩm chuyên nghiệp trong màn hình quản trị.

### 2. Tối ưu hóa Lưu trữ & Backend
- [x] **Multiple Upload Service**: Cấu hình Spring Boot xử lý danh sách `MultipartFile`, mã hóa tên file bằng UUID để tránh trùng lặp.
- [x] **Cơ chế Dọn dẹp File vật lý**:
    - Tự động xóa file trong folder `uploads` khi xóa sản phẩm khỏi Database.
    - **Fix lỗi nhân đôi file**: Triển khai logic thay thế (Replace) toàn bộ bộ ảnh cũ khi người dùng cập nhật ảnh mới, giúp folder `uploads` luôn gọn gàng.

### 3. Sửa lỗi hệ thống (Bug Fixes)
- [x] **Sửa lỗi Tràn số (Arithmetic Overflow)**: Nâng cấp kiểu dữ liệu của cột `price` trong Database (từ `precision 10` lên `18`) để hỗ trợ nhập giá tiền lên đến hàng tỷ VNĐ mà không bị lỗi.
- [x] **Fix lỗi Revert UI**: Khôi phục toàn bộ giao diện và logic Javascript sau khi file bị ghi đè nhầm về bản cũ.

---

## 🛠️ Công nghệ & Thư viện đã sử dụng
- **Frontend**: Tailwind CSS, Vanilla JS, Material Symbols, Swiper.js (CDN).
- **Backend**: Spring Boot, Spring Data JPA, Hibernate.
- **Database**: SQL Server.
- **Storage**: Local File System (`/uploads`).




